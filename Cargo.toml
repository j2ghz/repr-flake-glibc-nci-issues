[package]
name = "repr-flake-glibc"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
#openssl = "0"

[package.metadata.nix]
build=true
